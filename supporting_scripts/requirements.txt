# OpenWQ Supporting Scripts - Python Dependencies
# ================================================
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0
xarray>=0.20.0

# File I/O
h5py>=3.0.0
netCDF4>=1.5.0
imageio>=2.9.0

# Geospatial
geopandas>=0.10.0
shapely>=1.8.0
fiona>=1.8.0
pyproj>=3.0.0
rasterio>=1.2.0
pyogrio>=0.4.0
contextily>=1.2.0

# Plotting
matplotlib>=3.4.0
tqdm>=4.60.0

# Web requests (for GRQA download)
requests>=2.25.0

# Machine learning (for ML-based source/sink)
scikit-learn>=1.0.0
xgboost>=1.5.0
joblib>=1.1.0

# Sensitivity analysis (optional but recommended)
SALib>=1.4.0

# JSON schema validation (optional)
jsonschema>=4.0.0

# ─────────────────────────────────────────────────────────────────────────
# Browser-side dependency (NOT a pip package)
# ─────────────────────────────────────────────────────────────────────────
# The interactive HTML reports (config viewer + calibration results report)
# embed Leaflet 1.9.4 directly from the unpkg CDN:
#     https://unpkg.com/leaflet@1.9.4/dist/leaflet.{js,css}
#
# This requires INTERNET ACCESS from the machine that opens the report in
# its browser.  If you regularly view reports on an air-gapped HPC login
# node or behind a strict firewall, mirror the two files locally and patch
# the <link>/<script> tags in the report generators:
#   - 1_Model_Config/config_support_lib/Gen_Report.py
#   - 3_Calibration/calibration_lib/Gen_Calibration_Results_Report.py
# (search for "unpkg.com/leaflet" — there are 2 occurrences per file).
#
# Plotly.js is loaded the same way in the config viewer (also via unpkg).
